# Sample Sandbox Data

`demo_candidates.json` and `demo_candidates.jsonl` contain the same five anonymized candidate records.

Use them to verify the Streamlit sandbox locally:

```bash
streamlit run app.py
```

Then upload either sample file in the app.

These files are only for sandbox demos. They are not valid competition submissions and they do not represent the full 100k candidate pool.
