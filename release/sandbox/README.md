# Sandbox Package

The sandbox implementation is local-only in this repository and is not claimed as publicly deployed.

Primary files:

- `app.py`
- `sample_data/demo_candidates.json`
- `sample_data/demo_candidates.jsonl`
- `docs/sandbox_deployment.md`

Launch:

```powershell
streamlit run app.py
```
