1. Final CSV:
   `release/final_submission/Team_loading.csv`

2. Repository:
   `GitHub URL TODO`

3. PDF:
   `presentation/redrob_candidate_ranking_system.pdf`

4. PPTX:
   `presentation/redrob_candidate_ranking_system.pptx`

5. Metadata:
   `submission_metadata.yaml`

6. Sandbox:
   `TODO hosted URL`
   Local fallback: `streamlit run app.py`

7. Reproducibility artifacts:
   `release/RELEASE_MANIFEST.json`
   `release/RELEASE_MANIFEST.md`
